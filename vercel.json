{
  "version": 2,
  "builds": [
    { "src": "api/**/*.js", "use": "@vercel/node@20.x" },
    { "src": "package.json",  "use": "@vercel/static-build", "config": { "distDir": "dist" } }
  ],
  "rewrites": [
    { "source": "/api/:path*", "destination": "/api/:path*" },
    { "source": "/(.*)",       "destination": "/index.html" }
  ]
}
